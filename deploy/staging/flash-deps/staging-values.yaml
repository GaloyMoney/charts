strimzi-kafka-operator:
  enabled: false

kubemonkey:
  enabled: false

opentelemetry-collector:
  enabled: false

# cert-manager:
#   enabled: false
#   installCRDs: false
  # namespace: cert-manager # in terraform

ingress-nginx: 
  enabled: true
  # namespace: ingress # move to values.yaml
  controller:
    replicaCount: 1
    config:
      enable-opentracing: false