name: automerge
on:
  pull_request_review:
    types:
      - submitted
jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - name: automerge
        uses: "pascalgn/automerge-action@v0.14.3"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          MERGE_LABELS: galoybot
          MERGE_FILTER_AUTHOR: galoybot
          MERGE_FORKS: false
          MERGE_METHOD: squash
