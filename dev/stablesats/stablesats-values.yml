secrets:
  create: false
stablesats:
  galoy:
    phoneNumber: '+16505554350'
  exchanges:
   okex:
     client:
       apiKey: "dummy"
       simulated: true
  crash_report_config_danger: true

postgresql:
  auth:
    existingSecret: stablesats
    secretKeys:
      userPasswordKey: "pg-user-pw"
