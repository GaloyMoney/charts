{{ if (index .Values "admin-panel").enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "admin-panel.fullname" . }}
  labels:
    app: {{ template "admin-panel.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
spec:
  selector:
    matchLabels:
      app: {{ template "admin-panel.fullname" . }}
      release: {{ .Release.Name }}
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ template "admin-panel.fullname" . }}
        release: "{{ .Release.Name }}"
        {{- with (index .Values "admin-panel").labels }}
{{ toYaml . | trim | indent 8 }}
        {{- end }}
    spec:
      containers:
      - name: nginx
        image: "{{ (index .Values "admin-panel").image.repository }}@{{ (index .Values "admin-panel").image.digest }}"
        ports:
        - containerPort: 80
{{ end }}
