apiVersion: v1
kind: Secret
metadata:
  name: {{ include "price.fullname" . }}-config
  labels:
    {{- include "price.labels" . | nindent 4 }}
stringData:
  custom.yaml: |-
    {{- include "common.tplvalues.render" (dict "value" .Values.config "context" $) | nindent 4 }}
