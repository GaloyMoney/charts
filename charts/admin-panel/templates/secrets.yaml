{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "adminPanel.fullname" . }}
  labels:
    app: {{ template "adminPanel.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
type: Opaque
data:
  client-id: {{ .Values.secrets.googleOauthClientId | b64enc | quote }}
  client-secret: {{ .Values.secrets.googleOauthClientSecret | b64enc | quote }}
{{- end }
