apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-network" (include "lnd.fullname" .) }}
  labels:
    {{- include "lnd.labels" . | nindent 4 }}
stringData:
  network: {{.Values.global.network}}
