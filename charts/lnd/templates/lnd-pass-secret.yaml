apiVersion: v1
kind: Secret
metadata:
  name: {{ printf "%s-pass" (include "lnd.fullname" .) }}
  labels:
    {{- include "lnd.labels" . | nindent 4 }}
data:
  password: {{ template "walletPassword"  .  }}
