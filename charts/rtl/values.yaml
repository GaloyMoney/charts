# Default values for rtl.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# A random 8 digit password will be generated by default unless specified below
# password: password

replicaCount: 1

image:
  repository: shahanafarooqui/rtl
  tag: 0.10.2
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext:
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

service:
  port: 3000
  type: ClusterIP

# FIXME: Intake an array of lnds and secret names
lnd1Url: lnd1
lnd1SecretName: lnd1-credentials

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
