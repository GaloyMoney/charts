apiVersion: v1
kind: Service
metadata:
  name: {{ include "lnd-signer.fullname" . }}
  labels:
    {{- include "lnd-signer.labels" . | nindent 4 }}
  {{- with .Values.apiService.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.apiService.type }}
  {{- if and (eq .Values.apiService.type "LoadBalancer") (.Values.apiService.staticIP) }}
  loadBalancerIP: {{ .Values.apiService.staticIP }}
  {{- end }}
  ports:
    - name: rpc
      port: {{ .Values.apiService.ports.rpc }}
  selector:
    {{- include "lnd-signer.selectorLabels" . | nindent 4 }}
