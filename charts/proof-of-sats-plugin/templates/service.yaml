apiVersion: v1
kind: Service
metadata:
  name: {{ include "proof-of-sats-plugin.fullname" . }}
  labels:
    {{- include "proof-of-sats-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proof-of-sats-plugin.selectorLabels" . | nindent 4 }}
