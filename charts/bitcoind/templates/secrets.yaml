{{- if .Values.secrets.create }}
apiVersion: v1
kind: Secret
metadata:
  name: bitcoind-rpcpassword
  labels:
    {{- include "bitcoind.labels" . | nindent 4 }}
stringData:
  password: {{ .Values.bitcoindRpcPassword }}
{{- end }}
