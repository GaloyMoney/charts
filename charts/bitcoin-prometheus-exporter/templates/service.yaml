apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitcoin-prometheus-exporter.fullname" . }}
  labels:
    {{- include "bitcoin-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitcoin-prometheus-exporter.selectorLabels" . | nindent 4 }}
