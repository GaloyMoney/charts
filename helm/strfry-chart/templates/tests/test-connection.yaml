apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strfry-chart.fullname" . }}-test-connection"
  labels:
    {{- include "strfry-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "strfry-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
